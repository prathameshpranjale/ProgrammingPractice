#include<bits/stdc++.h>

using namespace std;
#define ll                long long
#define yes               cout<<"YES\n"
#define no                cout<<"NO\n"
#define FAST              (ios_base::sync_with_stdio(false),cin.tie(NULL))
#define sz(x)             (int)(x).size()
                                
int main()
{
   FAST;
   int t;
   cin>>t;

   while(t--){

    int n;
    cin>>n;
    vector<int>arr(n);
    vector<char>crr(n);
    for(int i=0;i<n;i++){
        int a;
        cin>>a;
        arr[i] = a;
    }

    for(int i=0;i<n;i++){
        char a;
        cin>>a;
        crr[i] = a;
    }


    
   }
       return 0;
}